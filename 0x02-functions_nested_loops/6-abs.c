#include"main.h"

/**
 * _abs - computes absolute value of an int
 * @n: Int to be computed
 * Return: 0 for success
 */

int _abs(int n)
{
	if (n < 0)
	{
		int abs;

		abs = -c;
		return (abs);
	}
	return (n);
}
