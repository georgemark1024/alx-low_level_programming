#include"main.h"

/**
 * _abs - computes absolute value of an int
 * @int: Int to be computed
 * Return: 0 for success
 */

int _abs(int)
{
	int x;

	if (int > 0)
		_putchar('n');
	else if (int == 0)
		_putchar('0');
	else
	{
		x = int * -1;
		_putchar('x');
	}
	return (0);
}
