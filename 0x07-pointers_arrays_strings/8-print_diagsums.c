#include "main.h"

/**
 *
 *
 *
 */


void print_diagsums(int *a, int size)
{
	int i = 0;

	while (i < size * size)
	{
		int inner_count = 0;

		while (inner_count < size)
		{
			 
			sum = sum + a[i][inner_count + size + 1];
			i = i + size + 1;
		}
	}
}
